# In a new powershell window run:
cd ~\python3\Scripts\
./activate
cd ~\source\django-geospacial\
# this will be used for local python testing only, everything else in git bash
# pycharm is also running powershell, so just use this

# you can run all your env config here
#(python3) PS C:\Users\David Saharkhiz\python3\Scripts> pip install Django
#etc etc